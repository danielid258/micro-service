spring:
  application:
    name: account
  cloud:
    config:
      uri: http://127.0.0.1:8888
      fail-fast: true
      password: 111
      username: user
      profile: dev #activates configuration in the account-dev.yml